---
- name: ensure ssmtp software is installed
  dnf: name=ssmtp
  sudo: yes

- name: ensure ssmtp is configured
  template: src=ssmtp.conf.j2
            dest=/etc/ssmtp/ssmtp.conf
            owner=root group=mail mode=0640
  sudo: yes
